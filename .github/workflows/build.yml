name: 'Test Build'

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: List Files
        run: ls
        shell: bash
      - name: Contents of image file
        run: more button-diff-ios-ltr-chromium-darwin.png
        shell: bash